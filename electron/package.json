{
  "name": "electron",
  "version": "1.0.0",
  "main": "./electron-js/main.js",
  "scripts": {
    "build": "tsc --project ./tsconfig.json",
    "start": "set isDev=1 && electron .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/electron": "^1.4.38",
    "electron": "^34.0.0"
  }
}
